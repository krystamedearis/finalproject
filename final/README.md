In ordert to pull built image from Dockerhub run:
docker pull krystamedearis:finalproject

In order to run the container use:
docker run -v finalproject:krystamedearis -it final

In order to view the image run:
docker images 
